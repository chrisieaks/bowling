one point for each knocked down pin;
if strike score = 10 + (next 2 balls);
if spare score = 10 + (next 1 ball);

state = {
    score : null,
    framesRemaining: 10,
    rolls: null,

}

if(10 % 2 === 0) {
    
}